{
  "name": "@flyo/nitro-next-workspace",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "./lib",
    "./playground"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=lib",
    "build": "npm run build --workspace=lib",
    "playground": "npm run dev --workspace=playground"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@flyo/nitro-js-bridge": "^1.1.2",
    "@flyo/nitro-typescript": "^1.1.0"
  }
}
